# raw/

Immutable source material. The agent reads from here and never edits it.

Put here: design notes, meeting and playtest transcripts, spec PDFs, data
exports, reference images, name lists — and filed research / options reports
(decision support for an open question, pointed to from `questions.md`).

When you add a source, ask Claude to fold it into the docs (greybox applies).
