# SITE-7 — Settlement access-fit registry integration

Status: answered
From: Settlements, Bases & Dungeons
To: Primary Builder — World & Terrain
Type: INTEGRATE
Notification: sent

## Request

Integrate the smallest truthful `world.settlement-generator` registry correction from
the independently reproduced RAID-5 counterexample. Do not create or promote a machine.

- The held-out seed-73 artifact emits ten ground-floor indoor neutral slots, but only
  two are reachable from any `path-centerline` space through declared `ground`
  connections. Four have zero edges and four remain in disconnected doorway fragments.
- The graph reproduces 43 weak movement components while the Raids production preflight
  still returns `ready-for-scenario`. Preflight proves evidence completeness and the
  clearance of retained edges; it does not prove complete site connectivity.
- Every current settlement building assembly requires a doorway and every ground-floor
  usable space receives a neutral slot. None is intentionally sealed.
- Falsified guarantee: truthfully omitting blocked links plus requiring one surviving
  exterior-to-interior route is insufficient. It permits a recipe to publish unexplained
  inaccessible indoor slots.
- Next isolated experiment: solve each required building-to-network access relationship
  from actual assembly footprint, doorway, threshold, terrain approach, and per-link
  movement envelope before publishing the coarse plan. Independently reject any recipe
  containing a neutral indoor slot without an explained applicable route.

Update only `world.settlement-generator` as appropriate:

- retain `active` status, current champion, wave, and dependencies;
- record the RAID-5 held-out failure in verifier/challenge/failure evidence;
- strengthen the promotion gate to require every emitted neutral indoor slot to be
  reachable through an applicable declared movement mode or carry an explicit neutral
  intentionally sealed physical fact;
- require zero silently omitted connections needed by emitted usable slots;
- do not treat Raids `ready-for-scenario` as proof of global topology connectivity;
- do not add tactical roles, raid suitability, or a new evaluator/machine.

Authorities:

- [`RAID-5`](RAID-5-disconnected-generated-interiors.md)
- [`generated-settlement-built-site-v1.json`](../../research/settlements-bases-dungeons/generated-settlement-built-site-v1.json)
- [`SITE-6`](SITE-6-settlement-physical-evidence-registry-integration.md)

Requirements checked: generated-content boundary and `BUILD-1`; exceptions: none.

## Response

- Accepted. Only `world.settlement-generator` was corrected. Its active status, champion,
  wave, dependencies, and ownership remain unchanged.
- The registry now records the seed-73 counterexample: `43` movement components, only
  `2/10` ground-floor indoor neutral slots reachable from the path network, four
  zero-edge interiors, and four disconnected doorway fragments. Raids
  `ready-for-scenario` is explicitly limited to retained-edge evidence and is not global
  topology proof.
- Promotion now requires every emitted neutral indoor slot to have an applicable
  declared route or an explicit neutral intentionally-sealed fact, with zero silently
  omitted connections required by usable slots. No tactical role, raid-suitability
  judgment, new evaluator, or new machine was added.
- The next access solver and regenerated artifact remain Settlements-owned work.

Requirements checked: generated-content boundary and `BUILD-1`; exceptions: none.

## Resolution

Pending.
